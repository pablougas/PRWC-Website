<div id="editform">
<%= bootstrap_form_for(@blog, html: { multipart: true }) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :title, hide_label: true, placeholder:'Post Title' %>
  </div>
  <div class="field">
    <%= f.text_area :description, hide_label: true, placeholder:'Post Content', rows: 16  %>
  </div>
    <%= f.hidden_field :user_id, value:current_user.id %>
  <div class="field">
    <%= f.file_field :avatar %>
    <small>Image dimensions must be 600px wide x 400px tall.</small>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
