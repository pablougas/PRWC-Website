<div class="nested-fields">
	<div class="ield">
		<%= f.collection_select :user_id, User.all, :id, :full_name %>
	</div>
	<p class="links">
    	<%= link_to_remove_association "Remove Assignee", f, class:"btn btn-warning btn-block" %>
  	</p>
</div>