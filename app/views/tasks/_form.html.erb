<div id="editform">
<%= bootstrap_form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
      <div class="col-md-12">
         <%= f.check_box :completed, label:"Mark Task as completed" %>
      </div>
      <div class="col-lg-9">
         <%= f.text_field :title %>
      </div>
      <div class="col-md-3">
         <%= f.date_select :deadline %>
      </div>
      <div class="col-md-12">
         <%= f.text_area :content %>
      </div>
      <div class="action">
         <%= f.submit %>
      </div>
   <% end %>
</div>
